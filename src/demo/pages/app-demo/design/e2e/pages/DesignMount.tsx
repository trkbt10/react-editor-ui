/**
 * @file DesignMount - Mount page for Design E2E testing
 */

import { DesignDemo } from "../../DesignDemo";
// Note: DesignDemo.tsx is in the parent directory (design/)

export default function DesignMount() {
  return <DesignDemo />;
}
