/**
 * @file Type definitions for GradientSection
 *
 * Gradient types are defined in utils/gradient/types.ts
 * Import directly from there:
 *   import type { GradientType, GradientStop, GradientValue } from "../../utils/gradient/types";
 */

// This file exists for consistency with other section structures.
// Types for gradients should be imported from utils/gradient/types.
